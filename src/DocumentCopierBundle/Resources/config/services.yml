services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    #
    # Controllers
    #

    DocumentCopierBundle\Controller\:
        resource: '../../Controller'
        public: true
        autowire: true
        tags: ['controller.service_arguments']

    DocumentCopierBundle\Controller\AdminController: ~

    #
    # Commands
    #

    DocumentCopierBundle\Command\:
        resource: '../../Command'
        tags: ['console.command']

    DocumentCopierBundle\Command\DocumentExportCommand:
        arguments:
            $kernelProjectDir: '%kernel.project_dir%'

    DocumentCopierBundle\Command\DocumentImportCommand:
        arguments:
            $kernelProjectDir: '%kernel.project_dir%'

    #
    # Services
    #

    DocumentCopierBundle\Service\:
        resource: '../../Service'

    DocumentCopierBundle\Service\FileService:
        arguments:
            $kernelProjectDir: '%kernel.project_dir%'

    DocumentCopierBundle\Service\AdminService:
        arguments:
            $kernelProjectDir: '%kernel.project_dir%'
